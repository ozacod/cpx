{
  "buildCommand": "cd web/frontend && npm install && npm run build && cd docs && npm install && npm run build && cp -r build ../dist/docs",
  "outputDirectory": "web/frontend/dist",
  "functions": {
    "api/index.go": {
      "maxDuration": 10
    }
  },
  "headers": [
    {
      "source": "/assets/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=0, must-revalidate"
        }
      ]
    }
  ],
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.go"
    },
    {
      "source": "/openapi.json",
      "destination": "/api/index.go"
    },
    {
      "source": "/((?!docs).*)",
      "destination": "/index.html"
    }
  ]
}